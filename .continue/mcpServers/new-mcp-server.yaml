name: mpc-server
version: 1.0.0
schema: v1
mcpServers:
  - name: mpc-server
    command: npx
    args:
      - mpc
      - server
      - --port=8080
      - --root-path=/music
    env:
      MPC_SERVER_PORT: 8080
      MPC_ROOT_PATH: /music

name: MPC Server Setup
version: 0.1.0
